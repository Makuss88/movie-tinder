import { styled } from '@mui/material/styles';

export const HeaderStyle = styled('button')({
    height: 48,
    textAlign: 'center',
    position: 'relative',
    overflow: 'hidden',
    zIndex: 4,
});
